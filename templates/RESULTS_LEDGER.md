# RESULTS LEDGER — 唯一数据真源(single source of truth)

> 规则(铁律10):任何论文/汇报里的数字必须出自本表,本表每行必须出自磁盘文件(路径已标)。
> 禁止凭记忆填数。新数字先 Read/Bash 从源文件读出再入表。更新时间戳 + 源文件。

## 主表(全测试集)— 源: <你的主结果文件,如 results.json / eval.log>
| 配置 | <指标1> | <指标2> | <指标3> | 源 |
|---|---|---|---|---|
| baseline(原版) | 待测 | | | <path> |
| proposed | 待测 | | | <path> |

## 入表规则
1. 每个数字必须能指向一个磁盘文件(*.json / *.log / *.ckpt文件名 / eval结果)。
2. 进行中的实验只填 checkpoint 实有的数字,没到的写"未到,等"。
3. 报均值±std+样本量(铁律5),全测试集不用子集。
4. 推断/预期值不混入主表,单独标"[预期]"。
5. ckpt 出处要可复现(写明 run 名 / epoch / 训练命令)。

## 消融/分析表(按需添加)
<!-- 模板:
## Exp-X: <实验名>(全测试集)— 源: <path>
| 配置 | <指标> | 源 |
|---|---|---|
| ... | ... | <path> |
结论: ...
-->
